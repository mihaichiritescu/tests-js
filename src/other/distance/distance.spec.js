QUnit.module('distance', {
  setup: function() {
    this.sandbox = sinon.sandbox.create();
  },
  teardown: function() {
    delete this.sandbox;
  }
});

QUnit.test("distance", function (assert) {
  
});